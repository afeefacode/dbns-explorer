const config = {
  brandColor: '#5FA93D',
  contrastTextColor: '#FFFFFF',
  regions: 'all',
  entities: [
    {
      type: 'event',
      filters: {
        'actors': ['432']
      }
    }
  ]
}

module.exports = config
